import { blogPost } from './blogPost'
import { category } from './category'
import { photoGallery, sinceLastTime } from './sinceLastTime'

export const schemaTypes = [blogPost, category, photoGallery, sinceLastTime]
